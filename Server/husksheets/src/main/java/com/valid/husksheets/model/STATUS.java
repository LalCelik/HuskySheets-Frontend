package com.valid.husksheets.model;

/**
 * Status of an Update
 */
public enum STATUS {
    /**
     * Published if Publisher updates
     */
    PUBLISHED,
    /**
     * Requested if Subscriber updates
     */
    REQUESTED
}
